# 14. Longest Common Prefix

\#Easy #String #Array #Trie

[toc]

## 1. Question

Write a function to find the longest common prefix string amongst an array of strings.

If there is no common prefix, return an empty string `""`.

 

**Example 1:**

> Input: strs = ["flower","flow","flight"]
> Output: "fl"

**Example 2:**

>Input: strs = ["dog","racecar","car"]
>Output: ""
>Explanation: There is no common prefix among the input strings.

 

**Constraints:**

- $$ 1 \leqq strs.length \leqq 200 $$
- $$ 0 \leqq  strs[i].length \leqq 200 $$
- `strs[i]` consists of only lowercase English letters if it is non-empty.

## 2. Solution

### 1) Vertical Scanning

- Time Complexity: $$ O(|strs[0| \cdot |strs|) $$
- Space Complexity: $$ O(|strs[0]) $$

#### A. C++

- Runtime: 0 ms
- Memory: 11.84MB
```C++
class Solution {
public:
    string longestCommonPrefix(vector<string>& strs) {
        if (strs.empty()){
            return "";
        }

        for(int i = 0; i < strs[0].length(); i++) {
            for(int j = 0; j < strs.size(); j++) {
                if(i == strs[j].length() || strs[j][i] != strs[0][i])
                    return strs[0].substr(0, i);
            };
        };
        return strs[0];
    }
};
```

#### B. Go

- Runtime: 0 ms
- Memory: 4.22MB
```Go
func longestCommonPrefix(strs []string) string {
    if len(strs) == 0 {
        return ""
    }

    first := strs[0]
    
    for i:= range first {
        c := first[i]
        for j := 1; j < len(strs); j++ {
            if i == len(strs[j]) || c != strs[j][i] {
                return first[:i]
            }
        }
    }
    return first
}
```
#### C. Python

- Runtime: 0 ms
- Memory: 19.22MB
```Python
class Solution:
    def longestCommonPrefix(self, strs: List[str]) -> str:
        if not strs:
            return ""

        first = strs[0]

        for i, c in enumerate(first):
            for j in range(1, len(strs)):
                if (i == len(strs[j]) or strs[j][i] != c):
                    return first[:i]
        return first
```

## Reference

1. LeetCode [14. Longest Common Prefix]
   - https://leetcode.com/problems/longest-common-prefix/
2. LeetCode Solutions [14. Longest Common Prefix] by P.-Y. Chen
   - https://walkccc.me/LeetCode/problems/14/